## Google Maps JavaScript API key. Needs the "Maps JavaScript API" and
## "Geocoding API" enabled in Google Cloud Console, and should be restricted
## by HTTP referrer to your dev/prod domains — Maps JS keys are always
## visible client-side, so referrer restriction is the real protection.
VITE_GOOGLE_MAPS_API_KEY=
